{
  "name": "react-boundingclientrects",
  "version": "1.0.0",
  "description": "Fetch boundingClientRects by selector for dangerouslySetInnerHTML",
  "main": "dist/index.js",
  "scripts": {
    "build": "mkdir -p dist && babel index.js -o dist/index.js",
    "start": "budo example.js -lov -- -t babelify",
    "prepublish": "npm run build"
  },
  "author": "Tim Hudson <tim.hudson15@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015": "^6.5.0",
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.2.0"
  },
  "dependencies": {
    "dom-event": "0.0.5",
    "react": "^0.14.7",
    "react-dom": "^0.14.7"
  }
}
